# Day 2 当日清单：面向对象（OOP）

> 对应总计划《第一周 · Day 2》。当天目标：把「封装/继承/多态、抽象类 vs 接口、sealed、static、readonly vs const、构造/静态构造、new vs override」全部吃透，三个 ⭐ 高频题能口述 + 手写。
> 配套练习代码：`Day02-练习代码/`（`cd Day02-练习代码 && dotnet run`）

---

## 一、当天要搞懂的 6 个知识点

| # | 知识点 | 一句话核心 | 重点动作 |
|---|---|---|---|
| 1 | 封装 / 继承 / 多态 | 封装藏细节、继承复用、多态按实际类型调用 | 写继承链 + 验证多态 |
| 2 | 抽象类 vs 接口 | 抽象类有部分实现且单继承，接口只定义契约且可多实现 | 跑 demo 看用法差异 |
| 3 | `sealed` | 阻止继承/重写，明确"到此为止" | 看编译报错演示 |
| 4 | `static` | 属于类型而非实例，全局共享一份 | 跑静态字段共享 demo |
| 5 | `readonly` vs `const` | const 编译期常量，readonly 运行期一次赋值 | 跑 demo 看赋值时机 |
| 6 | 构造/静态构造 + `new` vs `override` | 构造顺序；override 多态，new 只是隐藏 | 跑 demo 看调用顺序 |

---

## 二、知识拆解（含面试答案要点）

### 1. 封装 / 继承 / 多态

- **封装（Encapsulation）**：用 `private` 字段 + 公开属性/方法控制访问，把内部实现藏起来，外部只能通过约定好的接口交互。
- **继承（Inheritance）**：子类复用父类的字段和方法（`: Base`），C# 是**单继承**（一个类只能有一个直接基类）。
- **多态（Polymorphism）**：父类引用指向子类对象，调用 `virtual`/`override` 方法时按**实际类型**执行，即"一个接口，多种实现"。

**面试答案（一句话结论 → 展开 → 举例）：**
> 封装是隐藏内部实现、只暴露接口；继承是子类复用父类成员；多态是父类引用调用方法时按实际子类类型执行。三者是 OOP 的三大特性，多态需要 `virtual` + `override` 配合，而 `new` 关键字只是隐藏而非多态。

### 2. 抽象类 vs 接口 ⭐

**核心对照表：**

| 维度 | 抽象类（`abstract class`） | 接口（`interface`） |
|---|---|---|
| 成员实现 | 可以有已实现的方法、字段、属性 | 只有签名（C# 8+ 可有默认实现，但少见） |
| 继承数量 | **单继承**（一个类只能继承一个抽象类） | **多实现**（一个类可实现多个接口） |
| 能否有字段 | 可以（实例字段） | 不能有实例字段（只有属性/方法/事件/索引器） |
| 能否实例化 | 不能（只能被继承） | 不能（只能被实现） |
| 访问修饰符 | 成员可有多种修饰符 | 成员默认 `public` |
| 语义 | 描述"**是什么**"（is-a，一组相关类的公共基类） | 描述"**能做什么**"（can-do，一种能力/契约） |

**面试答案要点：**
> 抽象类是"是不是"的关系，接口是"能不能"的关系。抽象类可以有部分实现、有字段、单继承；接口只定义契约、不能有实例字段、可多实现。什么时候用：类之间有大量共性（代码复用）用抽象类；只定义能力、需要多实现（如 `IDisposable`、`IEnumerable`）用接口。

### 3. `sealed`

- 修饰**类**：不能被继承（防止被派生）。
- 修饰**方法**：该方法不能被进一步重写（必须配合 `override` 一起用，即 `sealed override`）。
- 用途：① 阻止意外扩展；② 编译器可对 `sealed` 类做少量优化；③ 某些设计（如值类型相关的类、性能关键类）明确禁止继承。

### 4. `static`

- **静态成员**：属于**类型本身**而非某个实例，所有实例共享同一份。
- **静态类**：不能实例化（`new` 会报错）、不能有实例成员，只能放静态成员；如 `Math`、`Console`。
- **静态构造函数**：类型第一次被使用前**自动执行一次**，用于初始化静态字段。

### 5. `readonly` vs `const` ⭐

| 维度 | `const` | `readonly` |
|---|---|---|
| 赋值时机 | **编译期**，声明时必须赋值 | **运行期**，声明或**构造函数**里赋值 |
| 类型限制 | 只能是基本类型 / `string` / `null` / 枚举 | 任意类型 |
| 隐式 static | 是（`const` 天然静态） | 否（可用 `static readonly`） |
| 引用类型 | 只能是字面量 | 可以是 `new` 出来的对象（但引用不可再变） |
| 作用 | 值在编译时就"写死"到各处 | 值在运行时确定后不可改 |

**面试答案要点：**

> const 是编译期常量，声明时必须赋值且只能用字面量、隐式 static；readonly 是运行期只读，可在构造函数里赋值、类型不限。需要"编译期就确定且不变"的用 const（如圆周率、固定配置），需要"运行时才确定但之后不变"的用 readonly（如从构造函数传入的依赖）。

### 6. 构造函数 / 静态构造函数 + `new` vs `override` ⭐

- **构造顺序**：静态构造函数（首次使用类型前，只一次）→ 基类实例构造函数 → 子类实例构造函数。
- **`override`**：真正重写虚方法，父类引用调用时**按实际类型**执行（多态）。
- **`new`**：只是**隐藏**父类同名方法，不是多态；父类引用调用时**按声明类型**执行，子类引用调用才走子类方法。

**面试答案要点：**
> override 是多态重写——父类引用指向子类对象时调用的是子类方法；new 只是隐藏——父类引用调用时仍走父类方法，只有用子类类型引用时才走子类方法。所以要实现多态必须用 override，new 只是"碰巧同名"的遮蔽。

---

## 三、手写练习（当日必做）

对应 `Program.cs`，每个都要能**自己手写一遍**（不照抄）：

- [x] 写一个 `Person` 类（private 字段 + 属性），一个 `Student : Person` 子类，演示继承
- [x] 写一个 `virtual` + `override` 方法，用父类引用调用子类方法验证多态
- [x] 写一个抽象类 + 一个接口，让一个类同时继承抽象类并实现接口
- [x] 写一个 `sealed` 类，尝试继承它看编译报错
- [x] 写一个静态字段，创建两个实例验证"共享同一份"
- [x] 写 `const` 和 `readonly` 各一个，说明赋值时机差异
- [x] 写 `override` 和 `new` 各一个，用父类引用调用，观察调用的是哪个方法

---

## 四、口述自测（当天结尾，用「结论 → 原理 → 例子」讲出来）

1. **面向对象三大特性分别是什么？多态怎么实现？**

   封装：用 `private` 字段 + 公开属性/方法隐藏内部实现，外部只能通过约定接口访问。
   继承：子类 `: 父类` 复用父类的属性和方法，C# 是**单继承**。
   多态：父类引用指向子类对象，调用 `virtual`/`override` 方法时按**实际类型（子类）**执行。

   > 注意：`Person p = new Student(...)` 只是「向上转型」，是**多态的前提**；真正体现多态的是随后 `p.MyInfos()` 调用的是子类重写后的实现，而不是父类方法。

2. **抽象类和接口的区别？什么时候用哪个？**

   区别：抽象类可以有已实现的方法和字段、单继承、描述「**是什么**」（is-a）；接口只有签名、不能有实例字段、可多实现、描述「**能做什么**」（can-do）。
   怎么选：要复用「字段 / 已实现的代码」用抽象类；只想定义「能力 / 契约」、要跨多个不相干类复用用接口。

   > 经验：优先接口（组合优于继承），确定「真的是同一族、要共享实现」才用抽象类。例：`Person`（有 `Name`/`Age` 共同字段）用抽象类；`IDisposable`/`IComparable` 这种「能力」用接口。

3. **`const` 和 `readonly` 的区别？**

   `const` 是**编译期**常量：声明时必须赋值、只能是基本类型 / `string` / `enum` / `null`、隐式 static（属于类型本身、不属于实例，所以用 `Person.Species` 访问）。
   `readonly` 是**运行期**只读：可在声明处或**构造函数**里赋值一次、类型不限（可以是 `new` 出来的对象）。

   > 补充：`const` 还能做方法内局部常量（`const int x = 5;`），`readonly` 只能做字段。

4. **`new` 隐藏方法和 `override` 重写的区别？**

   `override` 是「覆盖」：把父类方法**替换掉**，父类引用调用时也走子类（按**实例实际类型**，运行时多态）。
   `new` 是「并存」：子类**另起一个**同名方法，父类方法还在，父类引用调用时走**父类**（按**引用声明类型**，编译期决定），只有用子类类型引用才走子类方法。

   > 所以「用 new 时生成两个同名方法、引用时出错」是 new 的正常行为——它本来就不是多态。要实现多态永远用 `override`。

5. **（扩展）构造函数和静态构造函数的执行顺序？**

   静态构造函数先执行（类型首次使用前自动执行、只一次），之后才是实例构造函数；实例构造顺序是「基类实例构造函数 → 子类实例构造函数」。

---

## 五、完成标志 ✅

- [ ] 6 个知识点都能用自己的话讲出「一句话核心」
- [ ] `dotnet run` 跑通全部 6 个 demo，能解释每段输出
- [ ] 3 道 ⭐ 高频题能口述 + 手写关键代码
- [ ] 当天代码 `git add` + `commit` + `push`（养成习惯）
