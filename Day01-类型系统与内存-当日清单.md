# Day 1 当日清单：类型系统与内存

> 对应总计划《第一周 · Day 1》。当天目标：把「值类型/引用类型、装箱拆箱、string 不可变、StringBuilder、深浅拷贝」全部吃透，三个 ⭐ 高频题能口述 + 手写。
> 配套练习代码：`Day01-练习代码/`（`cd Day01-练习代码 && dotnet run`）

---

## 一、当天要搞懂的 6 个知识点

| # | 知识点 | 一句话核心 | 重点动作 |
|---|---|---|---|
| 1 | 值类型 vs 引用类型 | 值类型存数据本身，引用类型存指向堆上对象的地址 | 写对照表 + 改值验证 |
| 2 | 装箱 / 拆箱 | 值类型↔object 的转换，装箱在堆上新建对象 | 跑 demo 观察类型 |
| 3 | 栈 vs 堆 | 值类型多在栈，对象在堆，变量存地址 | 画一张图记住 |
| 4 | `string` 不可变 | 每次"修改"都生成新对象 | 理解线程安全好处 |
| 5 | `StringBuilder` | 内部可变缓冲区，避免频繁拼接的临时对象 | 跑性能对比 |
| 6 | 深/浅拷贝 | 浅拷贝复制引用，深拷贝复制内容 | 跑 demo 看差异 |

---

## 二、知识拆解（含面试答案要点）

### 1. 值类型 vs 引用类型 ⭐

**核心对照表：**

| 维度 | 值类型 | 引用类型 |
|---|---|---|
| 存储位置 | 栈上（或内联在对象内） | 对象在托管堆，变量（栈）存地址 |
| 赋值行为 | 复制「值」，两份独立 | 复制「引用」，指向同一对象 |
| 默认值 | `0` / `false` 等 | `null` |
| 继承 | 不能作为基类（隐式继承 ValueType） | 可继承 |
| 常见例子 | `int` `double` `bool` `struct` `enum` | `class` `string` `array` `delegate` `interface` |

**面试答案（一句话结论 → 展开 → 举例）：**
> 值类型直接存储数据本身，引用类型存储的是指向堆上对象的引用。因此值类型赋值是复制值、互不影响，引用类型赋值是复制地址、两个变量共享同一对象；值类型默认值为 0/false，引用类型默认 null。例如 `int b = a; b = 99;` 不影响 a，而 `int[] b = a; b[0] = 999;` 会同时改到 a。

### 2. 装箱 / 拆箱 ⭐

- **装箱（boxing）**：值类型 → `object`，隐式发生，在托管堆分配内存拷贝值。
- **拆箱（unboxing）**：`object` → 值类型，必须**显式强转回原始类型**，否则抛 `InvalidCastException`（如 int 装箱后不能拆成 long）。
- **性能损耗**：① 堆分配 + 增加 GC 压力；② 拷贝数据；③ 拆箱的类型检查。
- **如何避免**：用泛型（`List<int>` 而非 `ArrayList`）、避免把值类型赋给 `object`/接口。

**面试答案要点：**
> 装箱把值类型包装成堆上的对象（隐式发生），拆箱再取出来（需显式强转、类型必须一致）。损耗来自堆分配、数据拷贝和 GC 压力。避免办法是优先用泛型。

### 3. `string` 为什么不可变 ⭐

- 底层字符串内容是只读的，没有公开 API 修改内部字符。
- **好处**：线程安全（无需加锁）、可安全做字典/HashSet 的 key（哈希值稳定）、字符串驻留（interning）省内存。
- **坏处**：频繁 `+=` 产生大量临时对象 → 用 `StringBuilder`。

**面试答案要点：**
> string 一旦创建内容不可改，任何"修改"都返回新对象。好处是线程安全、可安全作 key、可驻留复用；坏处是频繁拼接性能差，循环拼接要用 StringBuilder。

### 4. StringBuilder

- 内部维护**可变**字符缓冲区，`Append` 在原有空间上追加，避免了 `+=` 的 O(n²) 临时对象。
- 规则：**循环/大量拼接用 StringBuilder；少量几次拼接用 `+`**（编译器会优化）。

### 5. 深拷贝 vs 浅拷贝（相关题库第 11 题）

- **浅拷贝**：只复制最外层，值类型字段复制，引用类型字段只复制引用（仍指向同一对象）。`MemberwiseClone()` 就是浅拷贝。
- **深拷贝**：引用类型字段也递归复制一份，完全独立。
- **实现深拷贝**：手动 `new` + 逐字段复制（引用字段用 `Clone()` 或递归），或用序列化反序列化（如 `System.Text.Json`）。

---

## 三、手写练习（当日必做）

对应 `Program.cs`，每个都要能**自己手写一遍**（不照抄）：

- [ ] 写一个值类型、一个引用类型，演示"改副本是否影响原变量"
- [ ] 写一次装箱 + 一次拆箱，标出哪行装箱、哪行拆箱
- [ ] 用 `Stopwatch` 对比 `List<int>` 和 `ArrayList` 的性能
- [ ] 证明 `s + " World"` 不改变原字符串 `s`
- [ ] 用 `Stopwatch` 对比 `+=` 和 `StringBuilder`
- [ ] 写一个含引用字段的类，实现 `ShallowCopy()` 和 `DeepCopy()`，验证差异

---

## 四、口述自测（当天结尾，用「结论 → 原理 → 例子」讲出来）

1. 值类型和引用类型的区别？各举几个例子。
2. 装箱拆箱为什么有性能损耗？如何避免？
3. `string` 为什么不可变？大量拼接用什么？
4. （扩展）深拷贝和浅拷贝的区别？怎么实现深拷贝？

---

## 五、完成标志 ✅

- [ ] 6 个知识点都能用自己的话讲出「一句话核心」
- [ ] `dotnet run` 跑通全部 6 个 demo，能解释每段输出
- [ ] 3 道 ⭐ 高频题能口述 + 手写关键代码
- [ ] 当天代码 `git add` + `commit` + `push`（养成习惯）
