# .NET 工程师复习 + 跳槽面试 + 实战进阶计划（1–3 个月）

> 面向：有 .NET 基础、毕业多年、目标跳槽，需系统重学 C# + 最新技术栈 + 面试过关 + 实战项目
> 版本基线：.NET 8（LTS）/ 9（STS）/ 10（LTS），C# 12 / 13 / 14
> 总周期：约 3 个月（第 1 个月约 5 周「复习」，第 2、3 个月「实战」）

## 三阶段总览

| 阶段 | 时间 | 定位 | 产出 |
|---|---|---|---|
| **阶段一：系统复习** | 第 1 个月（约 5 周） | 只学不写的「打基础」阶段，结束「只看计划」 | C# 语法/特性 + 数据库 SQL + ASP.NET/EF 吃透 + 面试题库 + 一个 API 项目 |
| **阶段二：实战编码①** | 第 2 个月（4 周） | 边学边写，**微服务架构**真实落地 | 一个完整微服务系统（多服务 + 网关 + 消息 + 可观测） |
| **阶段三：实战编码②** | 第 3 个月（4 周） | 边学边写，**AI Agent 应用开发** | 一个可展示的 Agent 应用 + 系统设计面试准备 |

> 每天 2–3 小时。时间紧张时：阶段一必做，阶段二、三可并行或延后，但建议按顺序走完。

---

# 阶段一：系统复习（第 1 个月，约 5 周）

> 目标：把 C# 语法、特性、原理、数据库 SQL、ASP.NET Core / EF Core 全部捡回来，结束「只看计划」阶段，为第 2 个月实战打底。每天标注面试高频考点。

## 第 0 天：环境准备（半天）

- [ ] 安装 [.NET SDK 10](https://dotnet.microsoft.com/download)（LTS，向下兼容 8/9）
- [ ] 安装 [Visual Studio 2022](https://visualstudio.microsoft.com/) 或 [VS Code + C# Dev Kit](https://code.visualstudio.com/)
- [ ] 安装 [Git](https://git-scm.com/) + GitHub 账号（练习代码和简历项目都放上面）
- [ ] 安装数据库客户端：[SQL Server](https://www.microsoft.com/sql-server/sql-server-downloads)（Developer 版免费）或 PostgreSQL；配一个图形化工具（SSMS / DBeaver / DataGrip）
- [ ] 练习项目数据先用 SQLite（EF Core 原生支持、零配置），SQL 练习用 SQL Server 或 PostgreSQL
- [ ] Docker 暂不装，第 2 个月做微服务时再装
- [ ] 验证：`dotnet --version`、`dotnet new console -o hello && cd hello && dotnet run`

**认知切换**：你以前学的多半是 .NET Framework（`System.Web`、`web.config`、IIS 专属）。现在主流是 **.NET Core 延续的 .NET 8/9/10**，跨平台、开源、命令行驱动。Day 1 专门讲区别，这是面试送分题。

### 第 1 周：C# 语言基础 + 面向对象（面试核心）

| 天 | 主题 | 学习内容 | 面试高频考点 |
|---|---|---|---|
| Day 1 | 类型系统与内存 | 值类型 vs 引用类型；装箱/拆箱；栈 vs 堆；`string` 不可变性；`StringBuilder`；深/浅拷贝 | ⭐「值类型和引用类型的区别？」「装箱拆箱为什么有性能损耗？」「string 为什么不可变？」 |
| Day 2 | 面向对象 | 封装/继承/多态；`abstract` vs `interface`；`sealed`；`static`；`readonly` vs `const`；构造/静态构造；`new` vs `override` | ⭐「接口和抽象类的区别？」「const 和 readonly 的区别？」「new 和 override 的区别？」 |
| Day 3 | 集合与泛型 | `List`/`Dictionary`/`HashSet`/`Queue`/`Stack`；泛型约束；协变 `out`/逆变 `in`；`IEnumerable` vs `IList` vs `ICollection` | ⭐「List 和数组的区别？」「Dictionary 底层原理？」「泛型协变逆变是什么？」 |
| Day 4 | 委托/事件/Lambda | 委托本质；`Action`/`Func`/`Predicate`；多播委托；事件 `event` vs 委托；Lambda；闭包 | ⭐「委托和事件的区别？」「什么是闭包？」「事件为什么用 event 关键字？」 |
| Day 5 | LINQ | 查询 vs 方法语法；延迟执行；常用操作符；表达式树概念 | ⭐「LINQ 延迟执行是什么，有什么坑？」「IEnumerable 和 IQueryable 的区别？」 |
| Day 6 | 异常 + 特性 + 反射 | `try/catch/finally`；异常过滤器 `when`；自定义异常；`Attribute`；反射 | ⭐「异常处理最佳实践？」「反射的性能问题？」「特性有什么用？」 |
| Day 7 | 周总结 + 摸底 | 复习 + 手写代码 + 口述原理 | 做文末题库第 1–3 类自测 |

### 第 2 周：C# 进阶 + 新特性（面试拉开差距）

| 天 | 主题 | 学习内容 | 面试高频考点 |
|---|---|---|---|
| Day 8 | 异步编程 | `async/await` 底层（状态机）；`Task` vs `Thread`；`ValueTask`；`ConfigureAwait`；`async void` 陷阱；`IAsyncEnumerable` | ⭐⭐「async/await 底层怎么实现？」「Task 和 Thread 的区别？」「为什么不用 async void？」 |
| Day 9 | 多线程与并发 | `lock`/`Monitor`/`SemaphoreSlim`；死锁；并发集合；线程池 | ⭐⭐「lock 的底层原理？」「如何避免死锁？」「并发集合为什么安全？」 |
| Day 10 | 内存与 GC | GC 三代回收；`IDisposable`/`using`；终结器；`Span`/`Memory`；`ref struct`；结构体 vs 类 | ⭐⭐「GC 工作原理？」「using 和 Dispose 关系？」「Span 为什么不能放堆上？」 |
| Day 11 | C# 新特性(上) | 模式匹配；`record`/`record struct`；`init`/`required`；`with`；顶级语句；全局 using | ⭐「record 和 class 的区别？」「模式匹配好在哪？」「init 和 set 的区别？」 |
| Day 12 | C# 新特性(下) | 可空引用类型 NRT；索引范围 `^` `..`；默认接口方法；主构造函数；集合表达式；扩展方法 | ⭐「NRT 解决什么问题？」「默认接口方法？」「扩展方法的本质？」 |
| Day 13 | 设计模式 + SOLID | SOLID；单例（线程安全）；工厂；DI/IoC；装饰器；观察者 | ⭐⭐「SOLID 每字母含义？」「写线程安全单例？」「DI 的好处？」 |
| Day 14 | 周总结 + 模拟 | 异步/内存/新特性综合题 | 能解释所有 ⭐⭐ 考点并手写代码 |

### 第 3 周：数据库与 SQL（重点补课，EF Core 的前置）

> 你很久没碰 SQL 了，这周专门补回来。SQL 是后端面试常驻考点，也是 EF Core 的地基——先弄懂 SQL，再学 EF Core 会顺很多。数据库选 **SQL Server**（.NET 岗位最常见）或 **PostgreSQL**，两者 SQL 语法 90% 通用。

| 天 | 主题 | 学习内容 | 面试高频考点 |
|---|---|---|---|
| Day 15 | SQL 基础语法 | `SELECT`/`WHERE`/`ORDER BY`/`GROUP BY`/`HAVING`；聚合函数；JOIN 家族（`INNER`/`LEFT`/`RIGHT`/`FULL`/`CROSS`） | ⭐「LEFT JOIN 和 INNER JOIN 的区别？」「WHERE 和 HAVING 的区别？」 |
| Day 16 | 查询进阶 | 子查询（标量/表/相关子查询）；CTE（`WITH`）；窗口函数（`ROW_NUMBER`/`RANK`/`DENSE_RANK`）；`UNION` vs `UNION ALL`；`CASE WHEN`；`EXISTS` vs `IN` | ⭐⭐「窗口函数怎么用？」「UNION 和 UNION ALL 的区别？」「EXISTS 和 IN 的区别？」 |
| Day 17 | 索引 | B+ 树原理；聚簇索引 vs 非聚簇索引；联合索引 + 最左前缀；覆盖索引；回表；索引失效场景 | ⭐⭐「为什么用 B+ 树？」「联合索引最左匹配原则？」「哪些情况索引失效？」「什么是回表？」 |
| Day 18 | 事务与锁 | ACID；四种隔离级别；脏读 / 不可重复读 / 幻读；MVCC；悲观锁 vs 乐观锁 | ⭐⭐「隔离级别有哪些？」「脏读、幻读、不可重复读的区别？」「乐观锁和悲观锁的区别？」 |
| Day 19 | 数据库设计 | 三大范式；反范式；主键/外键/约束；索引设计实战 | ⭐「三大范式是什么？」「什么时候反范式？」 |
| Day 20 | 性能优化 | 执行计划 `EXPLAIN`；慢查询定位；深分页（大 OFFSET）优化；大表优化思路 | ⭐⭐「怎么看执行计划？」「深分页怎么优化？」「慢查询怎么定位？」 |
| Day 21 | 周总结 + 实战 | 为图书系统设计表结构 + 手写复杂查询 | 写 JOIN/分组/窗口函数查询，建索引并用执行计划验证 |

**本周项目**：为「图书管理系统」设计完整表结构（用户 / 图书 / 借阅，含多对多关系），手写各种 JOIN、分组、窗口函数查询，给高频查询建索引并用 `EXPLAIN` 验证索引是否命中。

### 第 4 周：ASP.NET Core + EF Core（后端面试核心）

> 建立在第 3 周 SQL 基础上。EF Core 本质是把 SQL 查询翻译成 LINQ，SQL 功底越好，EF Core 用得越顺、越能理解它生成的 SQL。

| 天 | 主题 | 学习内容 | 面试高频考点 |
|---|---|---|---|
| Day 22 | ASP.NET Core 基础 | 启动流程；中间件管道（洋葱模型）；请求管线 | ⭐⭐「中间件执行顺序？」「请求怎么走完整条管线？」 |
| Day 23 | DI + 配置 | `IServiceCollection`；三种生命周期；`IConfiguration`/`IOptions` | ⭐⭐「三种生命周期区别？」「Singleton 注入 Scoped 会怎样？」 |
| Day 24 | Web API | Minimal API vs Controller；路由；模型绑定；Swagger/OpenAPI；过滤器 | ⭐「Minimal API 和 Controller 怎么选？」「模型绑定原理？」 |
| Day 25 | EF Core 基础 | `DbContext`；Code First + 迁移；关系配置；LINQ 查询；对照生成的 SQL | ⭐⭐「EF Core 如何追踪实体？」「AsNoTracking 作用？」 |
| Day 26 | EF Core 进阶 | `Include`；投影 `Select`；`ExecuteUpdate`/`ExecuteDelete`；懒加载；N+1 问题；查看生成的 SQL | ⭐⭐「N+1 问题怎么解决？」「懒加载和显式加载区别？」 |
| Day 27 | 认证与授权 | JWT；`Authentication` vs `Authorization`；策略授权；全局异常处理；`FluentValidation` | ⭐⭐「JWT 组成和工作流程？」「认证和授权区别？」 |
| Day 28 | 周总结 + 项目 | 综合复习 | 完成「图书管理系统 API」 |

### 第 5 周：工程化 + 面试冲刺

| 天 | 主题 | 学习内容 |
|---|---|---|
| Day 29 | 测试 | xUnit；FluentAssertions；Moq；Testcontainers；WebApplicationFactory |
| Day 30 | Docker + 通信 | Dockerfile；多阶段构建；docker-compose；gRPC/SignalR/BackgroundService 概念 |
| Day 31 | 可观测性 + 微服务概念 | OpenTelemetry；Serilog；.NET Aspire；微服务拆分/网关/服务发现 |
| Day 32 | CI/CD + 云部署 | GitHub Actions；Azure App Service / Container Apps 或 AWS ECS + RDS |
| Day 33 | 面试冲刺① | 把文末题库全部过一遍，能口述原理 + 手写关键代码 |
| Day 34 | 面试冲刺② | 整理项目经历（STAR 结构）+ 打磨简历 |
| Day 35 | 复盘 + 查漏 | 标出薄弱点，进入第 2 个月实战 |

**阶段一项目**：图书管理系统 API（JWT + CRUD + 借阅多对多 + EF Core + Swagger + 测试 + 可选容器化），这是你面试要讲的第一个项目。

> ✅ **阶段一完成标志**：能独立解释并手写 C# 核心语法、异步原理、SQL 查询、DI 生命周期、EF Core 查询；文末题库能答出 80%。此时「只看计划」阶段结束。

---

# 阶段二：实战编码① —— 微服务架构（第 2 个月）

> 目标：边学边写，把一个「微服务系统」真实落地。这是面试「系统设计/架构」题的直接素材。

| 周 | 主题 | 实战内容 | 技术栈 |
|---|---|---|---|
| 第 6 周 | 微服务基础拆分 | 把图书系统拆成 3 个服务：用户服务 / 图书服务 / 借阅服务；API 网关统一入口；服务间通信 | ASP.NET Core、YARP 网关、HTTP/gRPC、Docker |
| 第 7 周 | 微服务进阶 | 消息驱动（下单→扣库存→通知）；异步通信；Saga/Outbox 模式；服务发现与健康检查 | RabbitMQ（或 Azure Service Bus）、MassTransit、Consul |
| 第 8 周 | 微服务工程化 | 分布式追踪；统一日志；熔断限流；配置中心；容器编排 | OpenTelemetry、Serilog、Polly、docker-compose |
| 第 9 周 | 微服务收尾 | 完整跑通整套系统；写 README 架构图；部署到云 | GitHub Actions、Azure/AWS |

**阶段二产出**：一个多服务微服务系统（含网关、消息、可观测、CI/CD），可直接写进简历作为「架构能力」证明。

**面试关联考点**：微服务优缺点、服务间通信选型、分布式事务/Saga、熔断降级、网关作用、如何做分布式追踪。

---

# 阶段三：实战编码② —— 个人知识蒸馏 Agent（第 3 个月）

> 目标：做一个完整、可用、属于自己的 AI Agent——「**个人知识蒸馏 / 第二大脑**」。每次学到新知识、工作中遇到需要关注的内容，通过 agent 对话让它处理并存储，之后需要时检索调取**你自己整理的知识库**。这个项目天然涵盖 MCP、Skill、云存储、召回调优，是完整 Agent 工程，也是简历上最硬核的亮点。

## 项目定义（一句话）

> 一个对话式的个人知识库 Agent：输入杂乱的「学习笔记 / 工作备忘」→ LLM 蒸馏成结构化知识 → 存进向量库 + 云存储 → 需要时混合检索 + RAG 精准召回「我自己的知识」。

## 系统架构

```
┌──────────────────────────────────────────────────────────┐
│  输入层（多渠道 ingestion，通过 MCP 接入）                 │
│  对话输入 · 本地文件 · 网页 · 工作系统(Jira/文档)          │
└───────────────────────────┬──────────────────────────────┘
                            ▼
┌──────────────────────────────────────────────────────────┐
│  蒸馏层（Distill，LLM 处理）                              │
│  总结 · 提取要点 · 打标签 · 去重 · 关联 · 结构化           │
└───────────────────────────┬──────────────────────────────┘
                            ▼
┌──────────────────────────────────────────────────────────┐
│  存储层                                                   │
│  向量库(Qdrant/pgvector) · 元数据(Postgres/SQLite)        │
│  云对象存储(Blob/S3，存全文/附件)                          │
└───────────────────────────┬──────────────────────────────┘
                            ▼
┌──────────────────────────────────────────────────────────┐
│  召回层（Recall / RAG）                                   │
│  混合检索(向量+关键词) · 重排序(rerank) · 反馈闭环         │
└───────────────────────────┬──────────────────────────────┘
                            ▼
┌──────────────────────────────────────────────────────────┐
│  应用层：Agent 对话 · Skill 封装 · 多 Agent 协作 · 记忆     │
└──────────────────────────────────────────────────────────┘
```

## 核心技术栈

| 能力 | 技术选型 |
|---|---|
| LLM | OpenAI / Azure OpenAI / Claude（Anthropic API） |
| AI 编排 | Microsoft.Extensions.AI + Semantic Kernel |
| 工具接入 | MCP（Model Context Protocol） |
| 向量库 | Qdrant 或 pgvector（Postgres 扩展） |
| 元数据存储 | PostgreSQL / SQLite |
| 云存储 | Azure Blob Storage / AWS S3 |
| Embedding | text-embedding-3 系列 |
| 重排序 | cross-encoder reranker |
| 部署 | Docker + 云（Azure Container Apps / AWS ECS） |

## 每周计划

### 第 10 周：AI 基础 + 项目骨架 + 主链路打通

| 天 | 内容 |
|---|---|
| Day 1 | LLM 调用基础：OpenAI / Azure OpenAI SDK、流式输出、结构化输出（JSON mode / Function Calling） |
| Day 2 | Microsoft.Extensions.AI 统一抽象：`IChatClient`、Pipeline（中间件）、Embedding 抽象 |
| Day 3 | Semantic Kernel 入门：Kernel、Plugin（Function）、Prompt Template |
| Day 4 | 设计知识库 Schema：`KnowledgeEntry`（title / summary / content / tags / source / embedding / related / created_at） |
| Day 5 | 搭建项目骨架 + 数据模型 + 迁移 |
| Day 6 | **打通最小闭环**：对话输入 → LLM 蒸馏（总结+打标签）→ 存入 SQLite 元数据 + 生成 embedding |
| Day 7 | 周总结：跑通「存」这条链路 |

### 第 11 周：蒸馏 Pipeline + 存储设计

| 天 | 内容 |
|---|---|
| Day 1 | 蒸馏 Prompt 设计：总结、提取核心概念、自动打标签、判断是否与已有知识重复 |
| Day 2 | 去重与关联：向量相似度 + 标签匹配，新知识入库时关联已有相关条目（构建知识图谱边） |
| Day 3 | Chunking 策略：固定大小 / 递归 / 语义分块，对比效果 |
| Day 4 | 向量库接入：Qdrant（或 pgvector），`Upsert` 向量 + 元数据过滤查询 |
| Day 5 | 云存储接入：Azure Blob / S3，存知识原文和附件，元数据存引用 |
| Day 6 | 多源 ingestion：文件系统 MCP 读取本地笔记 → 走蒸馏 pipeline |
| Day 7 | 周总结：跑通「多源 → 蒸馏 → 向量库 + 云存储」完整入库 |

### 第 12 周：召回（RAG）+ 召回调优 + MCP

| 天 | 内容 |
|---|---|
| Day 1 | 混合检索：向量检索（dense）+ 关键词检索（sparse/BM25）组合 |
| Day 2 | RAG 问答链路：检索 Top-K → 拼 context → LLM 生成答案 → 附引用来源 |
| Day 3 | **召回调优**：重排序 rerank（cross-encoder）、元数据过滤、Top-K 与相似度阈值调参 |
| Day 4 | **召回评估**：建评估集，算 recall@k / MRR / nDCG，对比不同 chunking 和检索策略 |
| Day 5 | 反馈闭环：用户点赞/点踩 → 记录 → 影响后续排序（简单反馈学习） |
| Day 6 | MCP 深入：接入工作系统 MCP（如文档/工单），让 agent 主动拉取数据 |
| Day 7 | 周总结：跑通「检索 → 问答 → 反馈调优」闭环 |

### 第 13 周：Skill 封装 + 多 Agent + 记忆 + 落地

| 天 | 内容 |
|---|---|
| Day 1 | Skill 封装：把能力拆成可复用 skill——`distill`（蒸馏）/ `search`（检索）/ `organize`（整理）/ `review`（复习） |
| Day 2 | 多 Agent 协作：蒸馏 Agent、检索 Agent、整理 Agent 分工，一个 orchestrator 调度 |
| Day 3 | 记忆管理：短期（会话上下文）+ 长期（用户偏好、常用标签、知识偏好持久化） |
| Day 4 | 打磨体验：引用来源展示、知识去重提示、定期「复习推送」 |
| Day 5 | 部署：Docker 容器化 + 云部署，接入 CI |
| Day 6 | 写 README + 架构图 + 录 demo，整理成简历项目（STAR） |
| Day 7 | 项目复盘 + 阶段三总结 |

## 阶段三产出与面试价值

**产出**：一个完整可演示的「个人知识蒸馏 Agent」，涵盖 MCP、Skill、云存储、召回调优，是简历上最硬核的项目。

**面试关联考点**（新增高频题）：
- RAG 原理和流程？如何优化召回（召回调优）？
- 混合检索和纯向量检索的区别？
- Chunking 策略怎么选？
- Function Calling / Tool Use 机制？
- MCP 是什么，解决什么问题？
- Agent 和一次普通 LLM 调用的区别？
- 多 Agent 协作有哪些模式？
- 向量检索和关键词检索的区别？
- 如何评估一个 RAG 系统（recall@k / MRR）？

---

## 📌 面试高频题库（冲刺必背，按类别整理）

### 一、C# 语言基础
1. 值类型和引用类型的区别？各举几个例子。
2. 什么是装箱和拆箱？为什么有性能损耗？如何避免？
3. `string` 为什么是不可变的？大量拼接用什么？
4. 接口和抽象类的区别？什么时候用哪个？
5. `const` 和 `readonly` 的区别？
6. `new` 隐藏方法和 `override` 重写的区别？
7. 委托和事件的区别？事件为什么用 `event` 关键字？
8. 什么是闭包？
9. `IEnumerable` 和 `IQueryable` 的区别？
10. 泛型的协变和逆变是什么（`out`/`in`）？
11. 深拷贝和浅拷贝的区别？怎么实现深拷贝？
12. `readonly struct` 和普通 `struct` 的区别？

### 二、异步与多线程
13. `async/await` 底层是怎么实现的？（状态机）
14. `Task` 和 `Thread` 的区别？为什么用 Task？
15. `async void` 有什么问题？应该用什么替代？
16. `lock` 的底层原理（Monitor.Enter/Exit）？
17. 如何避免死锁？
18. `ConcurrentDictionary` 为什么线程安全？
19. `ValueTask` 和 `Task` 的区别？
20. `ConfigureAwait(false)` 有什么用？

### 三、内存与性能
21. .NET 的 GC 是怎么工作的？（三代回收）
22. `IDisposable`、`using`、终结器的关系？
23. `Span<T>` 和 `Memory<T>` 的区别？为什么 Span 不能放堆上？
24. 结构体和类在性能上的取舍？
25. 什么是可空引用类型（NRT），解决什么问题？

### 四、C# 新特性
26. `record` 和 `class` 的区别？
27. 模式匹配比传统写法好在哪？
28. `init`、`required` 分别解决什么问题？
29. 什么是顶级语句？
30. 默认接口方法有什么用？

### 五、ASP.NET Core
31. 中间件的执行顺序（洋葱模型）？
32. DI 三种生命周期的区别？Singleton 注入 Scoped 会怎样？
33. `Authentication` 和 `Authorization` 的区别？
34. JWT 的组成和工作流程？
35. ASP.NET Core 请求处理管线大致流程？
36. 如何实现全局异常处理？

### 六、数据库与 SQL
37. `LEFT JOIN` 和 `INNER JOIN` 的区别？
38. `WHERE` 和 `HAVING` 的区别？
39. `UNION` 和 `UNION ALL` 的区别？
40. 聚簇索引和非聚簇索引的区别？
41. 联合索引的最左前缀原则是什么？
42. 哪些情况会导致索引失效？
43. 事务的四大特性 ACID 分别是什么？
44. 数据库的四种隔离级别？各自解决/导致什么（脏读/幻读/不可重复读）？
45. 乐观锁和悲观锁的区别？
46. 数据库三大范式是什么？什么时候反范式？
47. 深分页（大 OFFSET）怎么优化？
48. 慢查询怎么定位和优化？

### 七、EF Core
49. EF Core 实体追踪机制？`AsNoTracking` 什么时候用？
50. 什么是 N+1 查询问题？怎么解决（`Include`/投影）？
51. 懒加载、显式加载、立即加载的区别？
52. 多对多关系怎么配置？

### 八、设计模式与架构
53. SOLID 五个原则分别是什么？
54. 写一个线程安全的单例模式。
55. 依赖注入的好处？
56. 微服务的优缺点？
57. 你如何设计一个高并发接口？

### 九、AI Agent（第 3 个月新增）
58. RAG 的原理和流程？
59. Function Calling 的机制是什么？
60. Agent 和一次普通 LLM 调用的区别？
61. 多 Agent 协作有哪些模式？
62. 向量检索和关键词检索的区别？

---

## 面试答题技巧

1. **原理题**：一句话结论 → 展开原理 → 举项目里的例子。
2. **写代码题**：先讲思路 → 再写代码 → 主动说边界和异常处理。
3. **SQL 题**：先讲思路 → 手写 SQL → 主动说索引和性能考量（这是加分项）。
4. **项目题**：STAR 结构（问题 → 职责 → 行动 → 结果数据）。
5. **系统设计题**（第 2 个月后）：先画架构 → 说拆分理由 → 说瓶颈和权衡（trade-off）。
6. **不会的题**：诚实说「了解不深，但我的理解是……」，展示思考过程比硬编加分。

---

## 关键版本速查表

| 项目 | 版本 | 说明 |
|---|---|---|
| .NET 运行时 | 8（LTS）/ 9（STS）/ 10（LTS） | 10 为当前最新 LTS |
| C# 语言 | 12 / 13 / 14 | 随 .NET 8 / 9 / 10 发布 |
| ASP.NET Core | 8 / 9 / 10 | Minimal API、内置 OpenAPI |
| EF Core | 8 / 9 / 10 | ORM |
| 数据库 | SQL Server 2022 / PostgreSQL 16 / MySQL 8 | .NET 岗位以 SQL Server 最常见 |
| 微服务 | YARP / MassTransit / RabbitMQ / Polly | 网关、消息、熔断 |
| AI Agent | Semantic Kernel / Microsoft.Extensions.AI / MCP | .NET 生态 AI 开发 |
| 测试框架 | xUnit + Testcontainers | 主流选择 |

---

## 贯穿全程的建议

1. **每天动手写**：面试是「写」出来的，每个考点都要能亲手写出来（含 SQL）。
2. **口述练习**：每天挑 3 道高频题，用「一句话结论 + 展开」讲出来。
3. **代码进 Git**：每天 push，三个月后就是完整作品集。
4. **一手资料**：`learn.microsoft.com` 是权威；SQL 部分可参考《SQL 必知必会》快速捡回语法。
5. **阶段一优先**：务必把 C# 基础 + SQL + ASP.NET/EF + 题库拿下，这是后面实战和面试的地基。
6. **Docker 放到第 2 个月**：做微服务时才真正需要，别让它挡第 1 个月的节奏。

---

*默认每天 2–3 小时。若时间紧张，必保：阶段一（含数据库周）+ 文末题库；阶段二、三可根据求职岗位（后端 vs AI 应用）调整权重。*
